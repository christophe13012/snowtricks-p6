{% extends 'base.html.twig' %}

{% block title %}Tricks
{% endblock %}

{% block body %}
	<div class="container p-5">
		<h1 class="display-4 text-center">Voici la liste des tricks</h1>
		{% if not app.session.get('user') %}
			<p class="lead mt-3">
				Pour ajouter ou éditer un trick, merci de vous identifier ou vous enregister.
			</p>
			<a href="{{ path('app_login') }}" class="btn btn-success">S'identifier</a>
			<a href="{{ path('app_register') }}" class="btn btn-primary">S'enregister</a>
		{% else %}
			<a href="{{ path('add') }}" class="btn btn-success">Ajouter un trick</a>
		{% endif %}
		{% if app.user %}
			<a href="{{ path('add') }}" class="btn btn-success mt-3">Ajouter un trick</a>
		{% endif %}
		{% include 'tricks/trickslist.html.twig' %}
	</div>
{% endblock %}
